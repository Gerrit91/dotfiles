################
### COLORS   ###
################

# source = ~/.config/hypr/myColors.conf

################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/

# @x-cellent
#monitor=DP-1,3840x2160@60.00Hz,auto,1.25
# @x-cellent kubus
#monitor=desc:Invalid Vendor Codename - RTK 0x1D1A 0x01010101,preferred,auto,auto,mirror,eDP-1
#monitor=desc:SVSI SVSi NSeries 0x01010101,preferred,auto,auto,mirror,eDP-1
monitor=desc:Sharp Corporation PN-R706 0x01010101,preferred,auto,auto,mirror,eDP-1

# @x-cellent
monitor=desc:Acer Technologies P5260E J60020016010,preferred,auto,auto,mirror,eDP-1

# @FI-TS
monitor=eDP-1,preferred,0x0,1
monitor=desc:HP Inc. HP Z24n G2 6CM0141CLJ,preferred,1920x0,1
monitor=desc:HP Inc. HP Z24n G2 6CM0141CCM,preferred,3840x0,1

monitor=eDP-1,preferred,0x0,1
monitor=desc:HP Inc. HP Z24n G2 6CM909242Z,preferred,1920x0,1
monitor=desc:HP Inc. HP Z24n G2 6CM90923W3,preferred,3840x0,1

# @home
monitor=desc:Dell Inc. DELL U3223QE 5H9X6P3,preferred,auto,1.5

# @notebook, should always be last entry as last entries have precedence
#monitor=eDP-1,preferred,auto-left,1
monitor=eDP-1,preferred,0x0,1

#############################
### ENVIRONMENT VARIABLES ###
#############################

# https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# env = GDK_BACKEND,wayland,x11,*
# env = GDK_SCALE,1

# env = SDL_VIDEODRIVER,wayland
# env = CLUTTER_BACKEND,wayland

# env = XDG_CURRENT_DESKTOP,Hyprland
# env = XDG_SESSION_TYPE,wayland
# env = XDG_SESSION_DESKTOP,Hyprland

# env = MOZ_ENABLE_WAYLAND,1
# env = HYPRLAND_TRACE,1

# env = QT_AUTO_SCREEN_SCALE_FACTOR,0
# env = QT_SCALE_FACTOR,1
# env = QT_QPA_PLATFORM,wayland;xcb
env = QT_QPA_PLATFORMTHEME,qt6ct

##################
###  PROGRAMS  ###
##################

$terminal = kitty
$fileManager = ADW_DISABLE_PORTAL=1 nautilus
$menu = walker
$browser = firefox

#################
### AUTOSTART ###
#################

exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = walker --gapplication-service
exec-once = udiskie & swaync & hyprpaper & hypridle & waybar &
exec-once = hyprpm reload

exec = gsettings set org.gnome.desktop.interface gtk-theme "adw-gtk3"
exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"

#####################
### GLOBAL STUFF  ###
#####################

# https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 20
    border_size = 1
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)
    resize_on_border = false
    allow_tearing = false
    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10

    active_opacity = 1.0
    inactive_opacity = 1.0

    #drop_shadow = true
    #shadow_range = 4
    #shadow_render_power = 3
    #col.shadow = rgba(1a1a1aee)

    shadow {
        enabled = true
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
# https://wiki.hyprland.org/Configuring/Animations/
animations {
    enabled = true
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# https://wiki.hyprland.org/Configuring/Dwindle-Layout/
dwindle {
    pseudotile = true
    smart_split = true
}

# https://wiki.hyprland.org/Configuring/Master-Layout/
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
}

debug {
  disable_logs = false
}

xwayland {
  force_zero_scaling = true
}

plugin {
    split-monitor-workspaces {
        count = 4
        keep_focused = 0
        enable_notifications = 0
    }

    # hyprbars {
    #     bar_height = 26
    #     bar_color = rgb(1e1e1e)
    #     col.text = rgb(ffffff)
    #     bar_text_size = 10
    #     bar_text_font = Font Awesome
    #     bar_button_padding = 10
    #     bar_padding = 10
    #     bar_precedence_over_border = true
    #     hyprbars-button = rgb(cc371d), 16, , hyprctl dispatch killactive
    #     hyprbars-button = rgb(ffd414), 16, ↗, hyprctl dispatch fullscreen 1
    #     hyprbars-button = rgb(398500), 16, ∿, hyprctl dispatch togglefloating
    # }

    # dynamic-cursors {
    #     enabled = true
    #     mode = rotate
    #     rotate {

    #         # length in px of the simulated stick used to rotate the cursor
    #         # most realistic if this is your actual cursor size
    #         length = 20

    #         # clockwise offset applied to the angle in degrees
    #         # this will apply to ALL shapes
    #         offset = 0.0
    #     }

    #     shake {
    #         enabled = false
    #     }
    # }

    # # use hyprcursor to get a higher resolution texture when the cursor is magnified
    # # see the `hyprcursor` section below
    # hyprcursor {

    #     # use nearest-neighbour (pixelated) scaling when magnifing beyond texture size
    #     # this will also have effect without hyprcursor support being enabled
    #     # 0 / false - never use pixelated scaling
    #     # 1 / true  - use pixelated when no highres image
    #     # 2         - always use pixleated scaling
    #     nearest = true

    #     # enable dedicated hyprcursor support
    #     enabled = true

    #     # resolution in pixels to load the magnified shapes at
    #     # be warned that loading a very high-resolution image will take a long time and might impact memory consumption
    #     # -1 means we use [normal cursor size] * [shake:base option]
    #     resolution = -1

    #     # shape to use when clientside cursors are being magnified
    #     # see the shape-name property of shape rules for possible names
    #     # specifying clientside will use the actual shape, but will be pixelated
    #     fallback = clientside
    # }
}

#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = de
    kb_variant = mac
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 2
    sensitivity = -0.5
    numlock_by_default = 1

    touchpad {
        natural_scroll = true
        # sensitivity = 0
    }
}

device {
    name = dll0945:00-06cb:cde6-touchpad
    sensitivity = 0
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = true
    workspace_swipe_distance = 150
    workspace_swipe_min_speed_to_force = 20
    workspace_swipe_cancel_ratio = 0.2
}

group {
    groupbar {
        font_family = Font Awesome
        gradients = true
        col.active = 0xBB000000
        col.inactive = 0x88000000
    }
}

####################
### KEYBINDINGSS ###
####################

$mainMod = SUPER

# General
bind = $mainMod, T, exec, $terminal
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, SPACE, exec, $menu
bind = $mainMod, B, exec, $browser
bind = $mainMod, W, killactive,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, ESCAPE, exit,
bind = $mainMod, V, togglefloating
bind = $mainMod SHIFT, V, workspaceopt, allfloat
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, split-workspace, 1
bind = $mainMod, 2, split-workspace, 2
bind = $mainMod, 3, split-workspace, 3
bind = $mainMod, 4, split-workspace, 4
bind = $mainMod, 5, split-workspace, 5
bind = $mainMod, 6, split-workspace, 6
bind = $mainMod, 7, split-workspace, 7
bind = $mainMod, 8, split-workspace, 8
bind = $mainMod, 9, split-workspace, 9
bind = $mainMod, 0, split-workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, split-movetoworkspace, 1
bind = $mainMod SHIFT, 2, split-movetoworkspace, 2
bind = $mainMod SHIFT, 3, split-movetoworkspace, 3
bind = $mainMod SHIFT, 4, split-movetoworkspace, 4
bind = $mainMod SHIFT, 5, split-movetoworkspace, 5
bind = $mainMod SHIFT, 6, split-movetoworkspace, 6
bind = $mainMod SHIFT, 7, split-movetoworkspace, 7
bind = $mainMod SHIFT, 8, split-movetoworkspace, 8
bind = $mainMod SHIFT, 9, split-movetoworkspace, 9
bind = $mainMod SHIFT, 0, split-movetoworkspace, 10

# bind = $mainMod SHIFT, left, movecurrentworkspacetomonitor, l
# bind = $mainMod SHIFT, right, movecurrentworkspacetomonitor, r

# move window to previous or next workspace
bind = $mainMod SHIFT, left,  movetoworkspace, e-1
bind = $mainMod SHIFT, right, movetoworkspace, e+1

# move window to other monitor
# bind = $mainMod CTRL SHIFT, left,  split-changemonitor, prev
# bind = $mainMod CTRL SHIFT, right, split-changemonitor, next

# Example special workspace (scratchpad)
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Resizing Windows
bind = $mainMod CTRL, right, resizeactive, 30  0
bind = $mainMod CTRL, left,  resizeactive, -30 0
bind = $mainMod CTRL, up,    resizeactive, 0   -30
bind = $mainMod CTRL, down,  resizeactive, 0   30

# Move Windows within a Workspace
bind = $mainMod ALT, right, movewindow, r
bind = $mainMod ALT, left,  movewindow, l
bind = $mainMod ALT, up,    movewindow, u
bind = $mainMod ALT, down,  movewindow, d

bind = CTRL SHIFT, left,  workspace, -1
bind = CTRL SHIFT, right, workspace, +1

bind = $mainMod, L, exec, hyprlock
bind = CTRL ALT, SPACE, exec, wofi-emoji
# bind = CTRL ALT, SPACE, exec, walker --modules emojis

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Fullscreen
bind= $mainMod, F, fullscreen,0
bind= $mainMod, Z, fullscreen,1

# Screenshots

bind = ,          PRINT, exec, hyprshot -m window
bind = $mainMod,  PRINT, exec, hyprshot -m output
bind = SHIFT,     PRINT, exec, hyprshot -m region

# Keyboard backlight
bind = , XF86MonBrightnessUp, exec, brightnessctl set +10%
bind = , XF86MonBrightnessDown, exec, brightnessctl set 10%-

# Volume and Media Control
bind = , XF86AudioRaiseVolume, exec, pulsemixer --change-volume +5 --max-volume 100
bind = , XF86AudioLowerVolume, exec, pulsemixer --change-volume -5
# bind = , XF86AudioMicMute, exec,
bind = , XF86AudioMute, exec, pulsemixer --toggle-mute
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPause, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous

bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# minimize window trick
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod, S, movetoworkspace, +0
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod, S, movetoworkspace, special:magic
# bind = $mainMod, S, togglespecialworkspace, magic

# Groups
bind = $mainMod, G, togglegroup

#bind = $mainMod, SUPER_L, overview:toggle, all

bind = $mainMod, Tab, cyclenext
bind = $mainMod ALT, Tab, cyclenext, prev

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/

workspace = w[tv1], gapsout:0, gapsin:0
workspace = f[1], gapsout:0, gapsin:0

windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
windowrule = rounding 0, floating:0, onworkspace:w[tv1]
windowrule = bordersize 0, floating:0, onworkspace:f[1]
windowrule = rounding 0, floating:0, onworkspace:f[1]

workspace = 6, name:coding, gapsout:0, gapsin:0, rounding:0, layoutopt:orientation:top, permanent_direction_override:true, smart_split:false
workspace = 9, name:terminal

windowrule = workspace 1, class:Slack
windowrule = workspace 2, class:org.keepassxc.KeePassXC
windowrule = workspace 5, class:firefox
windowrule = workspace 6, class:code
windowrule = workspace 6, class:code-oss
windowrule = workspace 9, class:kitty
windowrule = workspace 10 silent, class:GitKraken

windowrule = noblur, class:firefox
windowrule = noblur, class:code
windowrule = noblur, class:code-oss

windowrule = maximize, class:code
windowrule = maximize, class:code-oss

# windowrule = monitor eDP-1, class:^(org.keepassxc.KeePassXC)$
# windowrule = monitor eDP-1, class:^(Slack)$
# windowrule = monitor DP-3 , class:^(code-oss)$
# windowrule = monitor DP-3 , class:^(firefox)$
# windowrule = monitor DP-3 , class:^(code)$
# windowrule = monitor DP-3 , class:^(GitKraken)$

# windowrule = suppressevent maximize, class:.*
# windowrule = bordercolor rgb(FF0000) rgb(880808), fullscreen:1

#layerrule = blur,waybar

# windowrule = plugin:hyprbars:nobar, class:^(firefox)$
# windowrule = plugin:hyprbars:nobar, class:^(GitKraken)$
# windowrule = plugin:hyprbars:nobar, class:^(Chromium)$
# windowrule = plugin:hyprbars:nobar, class:^(Google-chrome)$
# windowrule = plugin:hyprbars:nobar, class:^(Slack)$
# windowrule = plugin:hyprbars:nobar, class:^(org.keepassxc.KeePassXC)$
# windowrule = plugin:hyprbars:nobar, class:^(kitty)$
